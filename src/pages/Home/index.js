import {View} from '@ant-design/react-native';
import {LayoutWrapper} from 'components';

function Home({navigation, StyleComputed, SingleComputed}) {
  return (
    <LayoutWrapper>
      <View>首页</View>
    </LayoutWrapper>
  );
}

export default Home;
